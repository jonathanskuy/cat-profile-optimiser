[theme]
base = "light"
primaryColor = "#E07856"           # warm terracotta — buttons, progress, accents
backgroundColor = "#fbebdb"        # soft warm cream
secondaryBackgroundColor = "#decab7"  # slightly deeper cream — sidebar/inputs
textColor = "#3D2E28"              # warm dark brown, high contrast
font = "sans serif"